import { SamplesTablePipe } from './samples-table.pipe';

describe('SamplesTablePipe', () => {
  it('create an instance', () => {
    const pipe = new SamplesTablePipe();
    expect(pipe).toBeTruthy();
  });
});
